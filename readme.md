#NS SNOW PACK APP

use latest node and yarn versions
```
$ yarn run start        # dev server
$ yarn run build        # build prod version
$ yarn run fmt          # reformat code
$ yarn run lint         # linting
$ yarn run test         # run unit tests
$ yarn run test:watch   # run unit test with watch
$ yarn run test:cov     # run unit test with code coverage
```
